import React from 'react';

const Child = ({arr}) => {
    return (
        <>
        <div>
            {arr.map(()=>{

            })}
        </div>
        </>
    );
};

export default Child;
